#!/bin/bash

cmd="$(python -c 'import sys,pipes;print(pipes.quote(sys.argv[1:]))' "$@")"
bin="$(python -c 'import sys,pipes;print(pipes.quote(sys.argv[1:]))' "$@")"
dir="$(python -c 'import sys,pipes;print(pipes.quote(sys.argv[1:]))' "$@")"
#clear
"$@"
echo "$cmd"
#watchmedo shell-command -c "clear; $command" 
